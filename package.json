{
  "name": "bms-frontend-monorepo-root",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "vite-react-template-main"
  ],
  "scripts": {
    "build": "cd vite-react-template-main && npm run build",
    "start": "cd vite-react-template-main && npm run preview -- --host 0.0.0.0 --port ${PORT:-3000}"
  }
}
